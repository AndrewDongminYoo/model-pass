import { defineConfig } from "@apps-in-toss/web-framework/config";

export default defineConfig({
  appName: "model-pass",
  brand: {
    primaryColor: "#a64436",
  },
  webView: {},
  permissions: [],
  webBundleDir: "dist",
});
